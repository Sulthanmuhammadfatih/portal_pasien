import 'package:flutter/material.dart';

class ListViewPoli extends StatelessWidget {
  const ListViewPoli({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
